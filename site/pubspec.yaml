name: site
description: A new Jaspr project
version: 0.0.1
publish_to: none

environment:
  sdk: '>=3.10.0'

dependencies:
  http: ^1.6.0
  jaspr: ^0.22.0
  jaspr_router: ^0.8.0
  shared:
    path: ../shared
  universal_web: ^1.1.1+1

dev_dependencies:
  build_runner: ^2.6.0
  jaspr_builder: ^0.22.0
  jaspr_lints: ^0.5.1
  build_web_compilers: ^4.4.6
  lints: ^5.0.0

jaspr:
  mode: server
  uses-flutter: false
